package com.viadeo.data.model

case class NetworkPhotoNews(
                             memberId: Int,
                             firstName: String,
                             lastName: String,
                             gender: Int,
                             hasPhoto: Boolean
                             )
